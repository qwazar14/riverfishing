# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea

# gradle
build
.gradle

# other
eclipse
run
runs
logs
.vscode

# OS
.DS_Store
Thumbs.db

# Files from Forge MDK
forge*changelog.txt
reference/

# python bytecode from the tools/ generators
__pycache__/

# agent worktrees (git worktree list still tracks them; this only keeps them out of git status)
.claude/worktrees/

# Hand-collected jars for a tester. Builds, not sources — they do not belong in history.
prerelease/
upload-*/

# generated hand-off pack for outside artists (rebuild with the script, never commit 8 MB of art)
art-pack-journal/
art-pack-journal.zip
